import HeaderBack from '@/app/(main)/profile/[username]/_components/HeaderBack';

type ProfilePageProps = {};

const ProfilePage = ({}: ProfilePageProps) => (
  <div className="w-[600px]">
    <HeaderBack />
    ProfilePage
  </div>
);

export default ProfilePage;
